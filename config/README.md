# Config Directory
- This directory will be linked to user home `.config` directory (a.k.a. `$HOME/.config`)
- Be careful to what you are doing when changing or linking files in `.config`  (try to back up everything in case)
